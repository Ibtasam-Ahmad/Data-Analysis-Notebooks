from django.contrib import admin
# from .models import Product

# Register your models here.

# @admin.register(Product)
# class ProductAdmin(admin.ModelAdmin):
#     list_display = ('title', 'price', 'link', 'updated_date', 'condition', 'rarity', 'game_name')
#     search_fields = ('title', 'game_name', 'ebay_id')  # Add fields you want to search by
#     list_filter = ('condition', 'rarity', 'game_name')  # Add fields you want to filter by
#     readonly_fields = ('ebay_id',)  # Make eBay ID read-only in the admin
